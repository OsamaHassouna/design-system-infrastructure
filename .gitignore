# Dependencies
node_modules/

# npm / package manager logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# OS artifacts
.DS_Store
Thumbs.db

# Editor directories
.vscode/settings.json
.idea/

# dist is COMMITTED — it is the distributable output of the build.
# Do not add dist/ here. Run `npm run build` to regenerate it.
# The build pipeline (Phase 9.5) produces deterministic output,
# so dist/ contents should always match what the pipeline emits.
