PHASE 6 — ADAPTER CONTRACT VALIDATION

Only after Button is stable.

--------------------------------------------------------------------
PROMPT:: 

Now design adapter contract and implement PrimeNG button adapter only.

Rules:

- Must not redefine tokens.
- Must only map token values to vendor classes.
- Must be removable without breaking system.

Explain:
- Adapter contract
- How new adapters follow same pattern.

--------------------------------------------------------------------
