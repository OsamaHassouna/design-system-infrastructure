PHASE 4 — CORE COMPONENT SYSTEM
STEP 4 — Build 3 Base Components (Button, Input, Card)

PROMPT:: 

Now implement component layer.

Rules:

- Components use ONLY CSS variables.
- No hardcoded values.
- No vendor selectors.
- No !important.
- Low specificity.
- All states included.

Implement:
- Button
- Input
- Card

For each:
- Show all states
- List all component tokens used
- Explain mapping between component and semantic tokens

--------------------------------------------------------

🧪 Required Tests

 -No color hex in components.

 -Only var(--...) usage.

 -All states implemented.

 -Clear separation from theme.

📊 Required Summary

Claude must explain:

 -Why this component structure scales

 -How new component tokens get added safely